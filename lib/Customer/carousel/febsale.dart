import 'package:flutter/material.dart';

class FebruaryHotDeals extends StatelessWidget {
  const FebruaryHotDeals ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hot Deals')),
      body: const Center(child: Text('Sale Coming Soon')),
    );
  }
}
